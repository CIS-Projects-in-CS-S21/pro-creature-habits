import Animated from 'react-native-reanimated';
import type { TabBarConfigurableProps } from '../types';
interface useTabBarItemFocusTransitionProps extends Required<Pick<TabBarConfigurableProps, 'duration' | 'easing'>> {
    index: number;
    selectedIndex: Animated.Value<number>;
}
export declare const useTabBarItemFocusTransition: ({ index, selectedIndex, duration, easing, }: useTabBarItemFocusTransitionProps) => Animated.Node<number>;
export {};
