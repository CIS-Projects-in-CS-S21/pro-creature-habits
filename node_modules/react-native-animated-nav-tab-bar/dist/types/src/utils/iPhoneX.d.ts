import { ScaledSize } from 'react-native';
export declare function isIphoneX(): boolean;
export declare function isIPhoneXSize(dim: ScaledSize): boolean;
export declare function isIPhoneXrSize(dim: ScaledSize): boolean;
